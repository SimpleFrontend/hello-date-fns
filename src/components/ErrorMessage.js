import React from 'react';
import { Icon } from 'antd';

const ErrorMessage = () => (
  <div>
    <Icon type="" />
    The API does not seem to work at the moment.
  </div>
);

export default ErrorMessage;
